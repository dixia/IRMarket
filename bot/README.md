# IRMarket Bot

> **Status:** Scaffold. Verification/settlement bot for IRMarket, built on the Monoracle
> veto-arbitrage primitive. Implementation follows the product requirement analysis.

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Configure environment
cp .env.example .env

# Run the bot
python verifier.py
```

## How It Works

(TODO — event monitoring, settlement price verification, veto/arbitrage logic)

## Configuration

See `.env.example` for all parameters.

## Production Deployment

(TODO — replace stubbed price logic with a real integration)