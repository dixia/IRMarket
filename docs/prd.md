# IRMarket 产品需求与UI设计全文档（V0.6 评审版）

> 本文档合并产品需求规格（PRD）与UI交互流程（UI Flow），用于整体逻辑评审、链路校验与落地对齐。
>
> **V0.6 变更说明**：基于 Monoracle 合约实际机制（`contracts/Monoracle.sol`，`github.com/dixia/monoracle`）对技术表述做了修正；标出认知误区、卡点（🚧）与待确认项（❓）；统一 Demo 期限为 **3 分钟**（详见 §八 D-01）。

## 阅读约定

| 标记 | 含义 |
|------|------|
| ⚠️ | **认知修正**：原文与 Monoracle 实际机制不符，已按合约源码纠正 |
| 🔁 | **复用 Monoracle**：该能力 Monoracle 已实现，IRMarket 直接调用，无需自研 |
| ✅ | **已确认决策**：原 ❓/🚧 已由用户拍板（见 §八 决策记录） |
| 🚧 | **卡点**：当前方案存在设计缺口，需要落地前解决 |
| ❓ | **待确认**：需求表述有歧义，需与用户确认后才可进入开发 |

> 版本状态：**V0.6 已收录用户对全部 ❓/🚧 的确认（见 §八）**。除 §八 内保留的细化项外，本文档可进入合约/前端细化。

---

## 〇、Monoracle 机制速览（认知基线）

> ⚠️ 以下为 Monoracle 合约源码中的真实行为。PRD 中涉及 Monoracle 的一切表述都应以此为准。

Monoracle **不是一个多节点/"预言家"聚合的预言机，也没有链下数据源**。它是一个"报价 + 免许可否决仲裁"的链上价格市场：

1. **报价（quote）**：provider 调用 `submitQuote(base, quote, baseAmount, quoteAmount)`，同时质押 **两种** ERC20（标的 base + 计价 quote），确定一个对价：
   `price = quoteAmount × 1e18 / baseAmount`（即"1 单位标的 = 多少 quote 计价币"，如 1 LLM = 若干 HKD）。
2. **双边抵押**：报价上链即锁定 provider 的 base 与 quote 双边抵押品。
3. **验证窗口**：固定 **2 slots（≈600ms，Monad 300ms 出块）**，窗口内报价为 `ACTIVE`。
4. **否决仲裁（veto）**：窗口内任何人可以对照自己掌握的"真实市场价"进行仲裁：
   - 报价偏低（real > quoted）→ `vetoUnderpriced`：仲裁者支付 quoteAmount，换走 baseAmount（低价买入真实更贵的标的）；
   - 报价偏高（real < quoted）→ `vetoOverpriced`：仲裁者支付 baseAmount，换走 quoteAmount（高价卖出真实更便宜的标的）。
   - 被否决的报价作废（`VETOED_*`），不进价格源。
5. **结算（settle）**：窗口内无人否决，则任何人（免许可）调用 `settleValidQuote`，该报价成为该资产对的 **canonical 有效价格**（`latestValidQuoteId` 指向它）。
6. **提取（withdraw）**：provider 事后退还——有效报价原额取回；被否决则 **加倍取回另一侧**（抵押惩罚，即否决-套利的风险/收益）。

**IRMarket 的借用边界（🔁）**：
- 实时参考价 / 结算价 = 调 `getLatestPrice(base, quote)`（Monoracle 已实现）
- 建市报价、否决、结算、提取 = 直接调用上述 Monoracle 函数（bot 脚本驱动，Monoracle 已实现）
- **IRMarket 需要自建的是**：期权数据结构、看涨/看跌开仓、提前平仓、资金池与盈亏计算、"以 Monoracle 价格作为输入的金融逻辑"。

> ⚠️ 因此：**用户买"看涨/看跌"期权 ≠ Monoracle 的 veto 方向**。用户是在 IRMarket 里交易期权仓位；veto 是链上任意套利者针对 **provider 的报价** 做的价格可信度仲裁，与用户的开仓方向完全无关（详见 2.1 修正）。

---

## 一、产品概述

### 1.1 产品定位

IRMarket 是部署在 Monad 区块链上的 **链上奇异期权市场**，支持任意具备公开价格的资产作为期权标的（例：A股、港股现货、Labubu），底层基于 **Monoracle 否决-套利原语** 提供价格结算，全程不依赖链下数据源与中心化验证节点。

产品UI与交互对标轻量化预测市场形态，用户仅需对标的价格做「看涨/看跌」的简单判断即可开仓，无需理解复杂期权术语。

### 1.2 示例标的定义（Demo 阶段）

| 项目 | 详情 |
|------|------|
| 标的全称 | 溜溜梅股份有限公司 |
| 上市市场 | 港交所主板 |
| 股票代码 | 06658.HK |
| 交易简称 | LLM |
| 业务属性 | 休闲食品研发、生产与销售，纯消费实体企业，与AI/大模型无业务关联 |
| 市场特性 | 因股票代码「LLM」与大语言模型缩写一致，引发散户非理性概念炒作，股价波动剧烈，具备典型情绪交易属性 |
| 选用价值 | 作为长尾非标资产示例，验证「任意有公开价格资产均可上链交易」的核心能力，同时降低普通用户认知门槛 |

### 1.3 核心特性

1. **去信任结算**：价格产生与结算完全基于 Monoracle 链上否决-套利机制（报价双边抵押 + 免许可仲裁），无链下数据源、无验证节点
2. **有限风险**：期权结构，无爆仓、无保证金追缴，用户最大亏损为投入本金（及手续费）
3. **灵活交易**：支持到期前反向平仓，随时锁定盈亏（✅ 对手方 = provider 抵押资金池，见 D-03）
4. **高性能底层**：基于 Monad 高性能 EVM 链，验证窗口仅 ~600ms（Monoracle 固定 2 slots）
5. **极简交互**：卡片式轻量化UI，核心操作一步直达，弱化区块链技术感

### 1.4 项目当前状态

- 脚手架搭建完成，目录结构对齐 Monoracle 项目；Monoracle 合约仅 **引用上游**（`github.com/dixia/monoracle`），本项目只保留 ABI（`abi/Monoracle.abi.json`）
- 核心产品规则与交互流程定稿（✅ 关键决策已确认，见 §八）
- Demo 版本进入开发准备阶段

---

## 二、核心业务规则

### 2.1 产品基础属性

- **产品类型**：欧式期权（仅到期按结算价兑付，支持到期前提前平仓）——✅ 盈亏模型 = **线性差价 PNL**（见 §八 D-02），非"奇异/二元"
- **期权期限**：可选时间到期。**Demo 默认固定 3 分钟**（✅ D-01；origin 原文 2.1 处写"demo 固定 1 分钟"，与 3.1 及 UI 的"3 个月"冲突，已统一）。Monad 出块 ~300ms，链上到期用 block 表达（3min ≈ 600 blocks）
- **交易方向**：同一标的两个方向——**看涨 / 看跌**。
  - ⚠️ **修正**：用户选「看跌」**不是** Monoracle 的 veto flow。用户买卖期权仓位；改用："结算时，看涨期权在 `结算价 > 开仓参考价` 时盈利，看跌期权反之"（见 2.4）。
- **结算资产**：链上统一使用测试币 `HKD` 作为计价/结算资产。⚠️ Monad 测试网无官方 HKD，需用可铸造 ERC20（`MockERC20`）发行 `LLM`（标的）与 `HKD`（计价）测试币；Monoracle 报价对 = `(base=LLM, quote=HKD)`，价格即 "HKD / LLM"
- **风险机制**：无爆仓、无保证金追缴、无强制平仓。用户最大亏损即开仓投入本金 + 手续费。⚠️ 期权本身无杠杆概念（最大亏损=本金），原文"大 leverage"已修正归入 PNL 模型（见 §八 D-02）
- **价格基准**：所有盈亏计算以 Monoracle canonical 价格为准（实时价 `getLatestPrice`；结算价同理）
- **开市权**：任何人可为任意标的创建市场（区别于 Polymarket 的平台审理制，Monoracle 支持任意资产对）——🔁 价格侧 Monoracle 已实现

### 2.2 做市商定价与手续费规则

> ⚠️ **区分两个价格**：Monoracle canonical 价 = 参考价（无升水、由否决仲裁约束真实）；IRMarket 的"成交价" = 参考价 ± 升水（IRMarket 自身的定价/手续费逻辑，Monoracle 不参与）。
>
> 🔁 做市商=Monoracle 的 provider：向 Monoracle 提交报价并双边质押（base+quote），获得否决风险收益（验证窗口内无人否决可原额取回，被否决则受损失）——**该机制 Monoracle 已实现，bot 直接驱动，无需 IRMarket 自研**。

1. **定价锚点**：做市商（provider）提交的报价严格锚定 溜溜梅（LLM）真實市场价（bot 抓取外部行情后调用 `submitQuote` 上链）
2. **报价升水机制（IRMarket 成交侧）**：前端展示给用户的期权交易价格，在参考价基础上上浮/下浮约 **1%**，以买卖价差形式隐性收取费用
   - 看涨开仓：成交参考价略高于市场参考价
   - 看跌开仓：成交参考价略低于市场参考价
   - ⚠️ 该"升水"是 IRMarket 定价层，**与 Monoracle 报价无关**；Monoracle 端报价就是真实市场价（否则会被否决套利惩罚）
3. **收益归属**：升水部分划转至做市商账户，作为做市商风险收益与对冲成本（✅ 在 IRMarket 池内单独记账结算，见 D-03）
4. **用户感知**：手续费不以单独字段展示，用户感知为交易滑点/买卖价差

### 2.3 交易与平仓规则

- **开仓**：用户选择方向、投入 QUOTE（HKD）数量，确认后链上锁定资金，生成持仓记录；持仓份数 = 投入 HKD ÷ 开仓参考价（✅ D-02）
- **反向平仓**：到期前可随时反向平仓锁定盈亏 ✅ **决策 D-03（Stage 1）**：对手方 = **price provider（做市商）持续报价并存入 Monoracle 的双边抵押资金**承接，按当前 Monoracle 参考价即时结算；后续（Stage 2）升级为分级基金模式
- **平仓定价**：平仓价格同样遵循 1% 升水规则，以实时参考价为基准（✅ 参考价由持续报价 bot 维持新鲜，见 D-05）
- **结算时效**：平仓交易上链确认后，盈亏实时结算，剩余资金自动划转至用户钱包

### 2.4 结算规则

- **触发条件**：期权到期后自动结算（✅ **决策 D-04**）。看涨/看跌用户仓位的盈利**由系统自动结算**（bot 免许可触发，仿 Monoracle settle 机制），**UI 实时展示浮盈**，可一键领取到账；结算价取结算tx执行时 `getLatestPrice` 的**最新 Monoracle 报价**
- **价格依据**：⚠️ 修正——Monoracle **没有"到期日最终有效报价"这一概念**，每个报价只对应一个验证窗口。IRMarket 结算价 = **结算交易执行时** 调 `getLatestPrice(base=LLM, quote=HKD)` 读取的最新 canonical 有效价（由 Monoracle 否决仲裁保证其真实性，🔁 已实现）
- **盈亏判定**（✅ 决策 D-02：**线性差价 PNL**，以 Monoracle 参考价差计算；用户确认"核心是算 PNL"，模型形状不纠结）：
  - 看涨：`盈亏 = (结算价 − 开仓参考价) × 持仓份数`；结算价 > 开仓价 → 盈利，反之亏损
  - 看跌：`盈亏 = (开仓参考价 − 结算价) × 持仓份数`；结算价 < 开仓价 → 盈利，反之亏损
  - 持仓份数 = 投入 HKD ÷ 开仓参考价（名义单位），PNL 封顶于本金（无追缴）
  - ⚠️ 原文"按差价兑付收益；反之本金全部亏损"的**二元表述已弃用**，统一为线性差价 PNL（见 §八 D-02）
- **资金兑付**：结算完成后，盈利资金自动划转至用户钱包地址

---

## 三、完整业务全流程

### 3.1 前置流程：做市商建市（Demo 阶段）

> ⚠️ 修正：这不是"向 Monoracle 写入初始市场价"这么简单——provider 必须 **双边质押** base+quote 两种 ERC20 提交真实价报价。完整建市如下：
>
> 1. （脚本）铸造 `LLM` / `HKD` 测试币，并在 Monoracle 上以 (base=LLM, quote=HKD) 建立报价对
> 2. （脚本=provider）抓取 LLM 真实市场价 → 按价格比率双边质押调用 `submitQuote`，报价上链（🔁 Monoracle 已实现，验证窗口 2 slots）
> 3. （可选）等待 `settleValidQuote` 使首笔价格成为 canonical（🔁 任何人可结算；bot 可代结算）
> 4. 调用 IRMarket 合约创建期限 = 3min（demo）的期权市场，注入初始 QUOTE 流动性
> 5. 市场生效，开放交易
>
> 后续加入分级基金机制（远期）。

> ❓→✅ **报价频率（决策 D-05）**：bot **持续报价**，频率以"**3 分钟 Demo 窗口内能完成一个完整流程周期**"为准（报价→窗口结算→用户开仓→平仓/到期→PNL 结算全链路走完 ≤ 3min）。每轮报价 2-slot 窗口（≈600ms）settle 后成为最新 canonical 价，供开仓/平仓/结算读取。

### 3.2 用户开仓流程（以看跌/做空为例）

1. 用户连接钱包，切换至 Monad 测试网（Chain ID 10143）
2. 在市场列表选择溜溜梅标的，进入详情页
3. 切换至「看跌」Tab，输入投入 QUOTE（HKD）数量
4. 系统实时计算持仓份数（= 投入 ÷ 开仓参考价）、预估盈亏、最大亏损（✅ D-02 线性差价模型）
5. 用户确认开仓，发起链上交易并签名
6. IRMarket 合约原子化执行：锁定用户资金 → 记录开仓参考价与持仓（参考价 = `getLatestPrice`）→ 计提升水
7. 交易上链确认，持仓生效

### 3.3 持仓与反向平仓流程

1. 用户在持仓页查看所有未平仓仓位与实时浮动盈亏（浮动盈亏 = f(当前 `getLatestPrice`，持仓参考价)，✅ D-02 线性模型）
2. 选择对应仓位，点击「反向平仓」（✅ D-03 provider 资金池接盘）
3. 弹出平仓确认卡片，展示平仓价格、预估盈亏
4. 用户确认后发起链上交易
5. IRMarket 合约按当前参考价计算平仓盈亏，扣除升水后划转资金
6. 对应持仓销毁，仓位关闭

### 3.4 到期结算流程

1. 期权到期后，持仓卡片「反向平仓」按钮替换为「结算」按钮（✅ D-04：到期由 bot 自动结算，UI 仅做展示/领取）
2. 系统展示最终结算价（= 结算 tx 时 `getLatestPrice` 的最新 Monoracle 报价，非"到期日历史报价"）与盈亏结果
3. bot 自动触发链上结算（任何人均可免许可触发）
4. IRMarket 合约读 `getLatestPrice`（🔁 Monoracle）计算最终盈亏
5. 兑付资金至用户钱包，仓位标记为已结算

---

## 四、分模块功能需求

### 4.1 智能合约层

#### 4.1.1 期权市场核心合约（IRMarket 自建）

- 创建固定期限期权市场，配置标的（base/quote ERC20）、到期时间（block）、做市商地址
- 实现看涨/看跌开仓、反向平仓、到期结算三大核心逻辑
- 持仓记账：开仓价、持仓数量、开仓时间、方向、市场引用
- 资金池管理：用户资金、盈亏资金、做市商收益分账户隔离（✅ 见 D-03）

#### 4.1.2 定价与手续费模块

- 对接 Monoracle 合约，读取实时参考价与结算价（`getLatestPrice`），所有盈亏计算基于该 canonical 价（🔁）
- 内置 1% 升水逻辑，开仓/平仓时自动计算成交参考价并计提做市商收益
- 支持配置做市商收益归集地址，手续费自动划转

#### 4.1.3 Monoracle 对接模块

- ⚠️ 修正：价格读取只有一个入口 = `getLatestPrice(base, quote)`，返回 (price, settledSlot, exists)。价格的有效性 = 链上否决仲裁机制保证，IRMarket **不需要**额外做"到期日有效报价"校验（Monoracle 无此概念）
- ⚠️ 需处理降级：查询时该资产对可能尚无 settled 报价（`exists=false`）——**前端禁用开仓/结算并提示"等待 bot 建立报价"**（✅ D-05 持续报价保证正常时段报价可用）
- 🔁 建市报价、否决、结算、提取全部复用 Monoracle 合约函数

### 4.2 做市商报价脚本（Demo 版）

- 基于 Monoracle 报价/结算脚本改造，单命令完成：铸币 → 提交报价（双边质押）→ 结算 canonical 价 → 创建 IRMarket 市场 → 注入流动性
- ⚠️ 修正：**不止是"写入初始价格"**，还承担 provider 双边质押与 window 内被否决的风控处理
- ✅ D-05：bot **持续报价**，频率适配 3min Demo 周期；命令行执行，无前端管理界面

### 4.3 前端功能模块

- 钱包连接与网络校验模块（Monad 10143）
- 标的市场展示模块
- 期权交易下单模块
- 持仓管理与平仓模块
- 一键结算模块
- 交易记录与资金流水模块

---

## 五、UI界面设计与交互流程（UI Flow）

### 5.1 整体设计规范

- **布局**：全程 Basic Card 卡片式布局，圆角卡片承载，信息层级扁平
- **配色**：主色明黄（品牌、核心按钮、重点数据）；绿色=看涨/盈利，红色=看跌/亏损；深灰底色
- **交互**：核心操作（开仓、结算）黄色主按钮，减少跳转层级
- **钱包**：Rabby、MetaMask 等主流 EVM 钱包，默认适配 Monad 链

### 5.2 全局核心链路总览

```
产品入口 → 市场列表页 → 标的详情页（看涨/看跌交易核心）
                          ↓
                    持仓管理页 → 一键结算（独立入口）
```
全页面顶部常驻导航栏，统一承载钱包连接、网络校验、资产余额。

### 5.3 分页面详细UI与交互

#### 5.3.1 全局顶部导航 + 钱包连接组件
- 左：IRMarket Logo + 产品名（黄色点缀）
- 右：网络状态标签 + 钱包按钮（未连接黄色「连接钱包」；已连接钱包缩略 + HKD 余额 + Monad Testnet 标签）
- 交互：选钱包 → 授权 → 校验链 ID（非 10143 弹「请切换至 Monad 测试网」+ 一键切换）；未连接时链上操作先走连接流程

#### 5.3.2 首页 / 市场列表页
- 卡片：标的名称「溜溜梅 LLM」+ 标签「港股食品股」+ 到期倒计时；中部当前价格（黄色大字体，来源：Monoracle `getLatestPrice`）+ 24h 涨跌幅；底部「看涨」（绿）「看跌」（红）+ 详情箭头
- 交互：点卡片→详情页；直接点看涨/看跌→详情页自动选中方向；未连接钱包先弹连接

#### 5.3.3 标的详情页（核心交易页）
- 左卡（标的信息）：名称/备注；当前链上价格（Monoracle 源）；到期信息 + 剩余倒计时；简易趋势占位
- 右卡（交易面板）：Tab 看涨/看跌（黄色选中态）；投入 HKD 输入框 + 可用余额；实时预估（份数 / 到期盈亏参考 / 最大亏损）；主按钮 绿=确认看涨开仓 / 红=确认看跌开仓；辅助说明文字
  - ⚠️ 原文案"欧式奇异期权 · 3个月到期 · 支持反向平仓 · 无爆仓风险" → 改为 **"欧式期权 · 支持反向平仓 · 无爆仓风险 · 到期自动结算"**（✅ D-01：Demo 默认 3 分钟，**期限选择器内保留其它期限选项**，默认选中 3min）
- 交互：切换 Tab 实时同步预估；输入金额实时计算；开仓按钮（无钱包→连接；签名→加载态→成功卡/失败卡）

#### 5.3.4 持仓管理页
- Tab：进行中 / 已结算
- 持仓卡：标的 + 方向标签；开仓价、当前价（Monoracle 实时价）、投入本金、浮动盈亏、到期倒计时；操作区 未到期=黄色描边「反向平仓」，到期=黄色高亮「结算」
- 交互：平仓确认卡 → 链上交易；到期自动切「结算」；「已结算」tab 查历史

#### 5.3.5 一键结算弹窗卡片
- 居中弹窗：标题黄色「结算确认」；标的名、方向、开仓价格、最终结算价；最终盈亏（盈绿亏红，黄色大字体）；全宽黄按钮「确认结算」
- 交互：核对 → 签名 → 成功态「资金已到账」/ 失败重试

#### 5.3.6 通用交易状态反馈组件
- 加载态 / 成功态（含 tx 哈希跳转）/ 失败态（含重试），全链路复用

### 5.4 核心场景完整流程走查
- 场景1 看跌开仓：列表→看跌→详情选中→输入→确认→签名→成功→查看持仓
- 场景2 反向平仓：持仓→反向平仓→确认卡→签名→资金到账
- 场景3 到期结算：持仓→已到期→结算→确认→签名→到账

---

## 六、Demo 版本范围界定

| 模块 | Demo 包含 | 暂不实现 |
|------|-----------|----------|
| 标的 | 单标的：溜溜梅（LLM）3 分钟期权（默认，UI 提供多期限选择 ✅ D-01） | 多标的、用户自定义标的 |
| 交易 | 看涨/看跌开仓、反向平仓（✅ D-03 provider 资金池接盘）、到期自动结算（✅ D-04） | 保证金、爆仓强平、限价单、止盈止损 |
| 做市商 | 脚本报价建市、**持续报价 bot（✅ D-05，3min 周期）**、固定 1% 升水 | 动态费率、多做市商接入、分级基金（Stage 2） |
| 价格 | ⚠️ 修正：Monoracle **报价式价格**（provider 双边抵押 + 否决仲裁，🔁 原生机制，非"单节点"） | 多报价聚合、分级基金 |
| 资产 | HKD / LLM 测试币（MockERC20 发行） | 多币种、稳定币、真实法币通道 |
| 前端 | 市场列表、交易详情、持仓管理、结算展示/领取（✅ D-04） | 高级K线、深度图、资产分析 |

---

## 七、评审校验清单（Review Checklist）

> ✅ = 已确认；⚠️ = 本次已修正；🚧/❓ = 待处理（均已给出建议或已拍板）

1. ✅ 产品形态：期权 + 预测市场轻量化交互（线性差价 PNL，D-02）
2. ✅ 标的定义：溜溜梅 LLM 港股食品股、概念炒作属性
3. ✅ 核心规则：3 分钟默认期限（UI 多期限选择 D-01）、无爆仓、反向平仓（D-03）、1% 升水手续费、到期自动结算（D-04）
4. ✅ 结算逻辑：价格 = `getLatestPrice`（🔁 Monoracle），bot 自动结算，UI 展示盈亏
5. ⚠️ 交易方向与 Monoracle 关系已修正：用户期权方向 ≠ Monoracle veto 方向
6. ✅ UI 流程：连钱包→开仓→持仓→平仓/结算全链路无断点（文案 3 个月→默认 3 分钟）
7. ✅ 设计风格：卡片式、黄色主色调、简洁
8. ✅ Demo 范围：持续报价 bot（D-05）、单标的、核心闭环
9. ❓ 是否存在其他遗漏的核心场景或异常流程（请在下方反馈）

---

## 八、决策与卡点记录（用户已确认 / 待细化）

> **状态**：全部原 ❓/🚧 已由用户于 V0.6 评审确认（✅ D-01 ~ D-05）。下方仅保留实现层面的细化说明。

### ✅ D-01 期权期限
- Demo 默认 **3 分钟**（≈600 blocks）；**UI 期限选择器需提供其它期限选项**（3min / 5min / 1h / 1d / 1month），默认选中 3min。合约支持多期限，Demo 主要走 3min 链路。

### ✅ D-02 盈亏模型（线性差价 PNL）
- 产品本质 = **基于 Monoracle 报价的期权**，核心是 **PNL 计算**，模型形状不纠结。
- 公式：`持仓份数 = 投入 HKD ÷ 开仓参考价`；看涨 `PNL = (结算价 − 开仓参考价) × 份数`，看跌反之；PNL 封顶本金（无追缴）。
- 结算价 / 平仓价 / 实时浮盈均读 `getLatestPrice`（Monoracle 最新有效报价）。
- 落地注意：结算价与开仓参考价的时间锚点（开仓时快照 vs 结算 tx 时最新报价）要在合约里显式记录，防止重入/抢跑。

### ✅ D-03 反向平仓对手方（Stage 1）
- 对手方 = **price provider（做市商）**：其通过 **持续报价** 存入 Monoracle 的**双边抵押资金**构成承接池，平仓按当前 Monoracle 参考价即时结算（用户卖回给池子）。
- 做市商风险敞口由 1% 升水收入 + 报价抵押收益覆盖。
- **Stage 2（远期）**：升级为 **分级基金（tranche fund）** 模式承接对手风险。
- 落地注意：pool 需在 IRMarket 内独立记账（用户资金 / 盈亏池 / 做市商收益三账户隔离）；池余额不足时需拒绝开仓或提示。

### ✅ D-04 到期结算触发
- **自动结算**：到期后由 bot（任何人均可免许可触发，仿 Monoracle settle）自动为看涨/看跌仓位结算盈利。
- **UI 只负责展示与领取**：展示最新 Monoracle 报价下的浮盈/最终盈亏，用户一键领取到账。
- 落地注意：自动结算的 gas 承担与奖励（小额返还）需要在合约中定义，防止无人触发的资金僵死。

### ✅ D-05 报价频率
- bot **持续报价**，频率以"**3 分钟 Demo 窗口内可完成一个完整流程周期**"为准（建市报价→窗口结算→用户开仓→平仓/到期→PNL 结算，≤ 3min 全部走完）。
- 每轮报价经 2-slot 验证窗口（≈600ms）settle 后成为最新 canonical 价；连续报价维持参考价新鲜，平仓/结算可信。
- 落地注意：报价 interval 需兼顾 gas（Monad 按 gas_limit 计费）与 3min 窗口；可在 `bot/` 内做成可配置参数。

### 细化项（非阻塞，随开发推进）
- 🚧-04 资产链路：自铸 `HKD` / `LLM` MockERC20（`script/deploy-tokens.js`），ORACLE/测试币地址已在 `.env.example` pin。**建议**：自铸语义清晰的 HKD/LLM，而非复用 monoracle 占位测试币（待开发时确认即可）。
- 分级基金（Stage 2）仅记录方向，不进 Demo 范围。