// TODO: mirror the Monoracle global setup — compile contracts, boot a local Hardhat
// node on 127.0.0.1:8545, deploy IRMarket + tokens, write web/.env.local, then teardown.
export default async function globalSetup() {
  console.log("[Setup] IRMarket e2e setup — implement after contracts ship (see E2E_IMPLEMENTATION_PLAN.md)");
}