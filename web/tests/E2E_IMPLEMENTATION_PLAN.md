# IRMarket — E2E Implementation Plan

> **Status:** TODO — write once the option-market contract + frontend ship. Mirror the
> Monoracle plan: wallet connect, lifecycle (quote → settle → veto → withdraw), helpers
> (deploy, abis, address fixtures, block mining, Hardhat→Playwright bridge).